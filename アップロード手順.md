# 本番公開手順（Xserver / Dreamweaver）

アップするのは **`public_html/` フォルダの中身** だけ。フォルダごとではなく、中身をサーバーの `g-tech-japan.co.jp/public_html/` 直下へ。

## 1. FTP アカウント（Xserver サーバーパネル）
- 「FTPアカウント設定」→ ドメイン g-tech-japan.co.jp → 追加
- 接続先ディレクトリは `/g-tech-japan.co.jp/public_html` にしておくと Dreamweaver 側のルート設定が楽

## 2. Dreamweaver サイト設定
- ローカルサイトフォルダー: `…/_自社システム/コーポレートサイト/public_html/`
- サーバー: FTP over SSL/TLS（FTPS）推奨、ホスト `svXXXX.xserver.jp`、ルートディレクトリ `/`（↑で接続先を public_html にした場合）

## 3. サーバー側の片付け（重要）
現在 public_html には **初期状態の WordPress** が入っている。
- `wp-admin/` `wp-content/` `wp-includes/` `wp-*.php` `xmlrpc.php` `.htaccess` `index.php` をサーバーから削除してからアップ
- 残しておくと放置 WordPress が攻撃対象になる。サーバーパネルの「WordPress簡単インストール」から削除してもよい
- 削除前に念のためバックアップを取ること（中身は空なので実質不要）

## 4. アップロード
`public_html/` の中身を全部上げる。
`.htaccess` は隠しファイルなので Dreamweaver の「隠しファイルを表示」で見えることを確認。

## 5. 確認
- https://g-tech-japan.co.jp/ でサイトが表示される
- http:// でアクセスしても https:// に飛ぶ（SSL設定が有効になっていること）
- https://g-tech-japan.co.jp/privacy.html
- 存在しないURLで 404 ページが出る

## 6. 以後の更新
ソースを直したら `public_html/` に同期してからアップ。Claude に「public_html に反映して」と言えば同期します。
